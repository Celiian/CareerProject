{% extends('base.html.twig') %}

{% block body %}

    <div>
        {{ form(form) }}

        <a href="{{ path('connected_company',{company_id: company_id} ) }}">
            <button>Go back</button>
        </a>
    </div>

{% endblock %}