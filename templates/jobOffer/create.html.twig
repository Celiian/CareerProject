{% extends('base.html.twig') %}

{% block body %}

    <div>
        {{ form(form) }}

        <a href="{{ path('connected_company',{id: id} ) }}">
            <button>Go back</button>
        </a>
    </div>

{% endblock %}