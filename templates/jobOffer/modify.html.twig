{% extends('base.html.twig') %}

{% block body %}

    {{ form(form) }}

    <a href="{{ path('company_candidate',{company_id: company_id} ) }}">
        <button>Go back</button>
    </a>

{% endblock %}