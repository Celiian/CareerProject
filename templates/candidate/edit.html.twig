{% extends('base.html.twig') %}

{% block body %}
   {{ form(form) }}


   <a href="{{ path('delete_candidate',{candidate_id: candidate_id} ) }}">
      <button>Delete your candidate</button>
   </a>

   <a href="{{ path('connected_candidate',{candidate_id: candidate_id} ) }}">
      <button>Go back</button>
   </a>
{% endblock %}