{% extends('base.html.twig') %}

{% block body %}
    <div>
        <h2>Connected at {{ company.name }}'s account</h2>
    </div>

    <a href="{{ path('edit_company',{company_id: company.id} ) }}">
        <button>Edit your company</button>
    </a>

    <a href="{{ path('create_job_offer',{company_id: company.id} ) }}">
        <button>Post a job offer</button>
    </a>

    <a href="{{ path('company_candidate',{company_id: company.id} ) }}">
        <button>See your job offer</button>
    </a>

    <a href="{{ path('delete_company',{company_id: company.id} ) }}">
        <button>Delete your company</button>
    </a>

    <a href="{{ path('connect_company') }}">
        <button>Go back</button>
    </a>
    </div>

{% endblock %}