{% extends('base.html.twig') %}

{% block body %}
    {{ block("nav", "./navs/navCompany.html.twig") }}

    <div>
        {{ form(form) }}
        <a href="{{ path('company') }}">
            <button>Go back</button>
        </a>
    </div>

{% endblock %}